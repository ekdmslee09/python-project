# 1. 기능 목록 (MUST,NICE,LATER)

```
[MUST] 점수를 입력받기 (사용자입력)
[MUST] 잘못된 데이터 예외 처리하기 (숫자가 아니거나 범위를 벗어난 점수)
[MUST] 기초 통계량 출력하기 (최소값, 제 1 사분위수, 평균, 중앙값(제 2 사분위수), 제 3 사분위수, 최대값)
[MUST] 성적 분포 시각화하기 (Histogram, Boxplot)
[NICE] 이상치 판단하기 (IQR 활용)
[LATER] 성적 분석 결과를 파일로 저장하기 
[LATER] 성적데이터를 순위와 함께 표 형식으로 출력하기
[LATER] 파일로 점수를 입력받기
[LATER] 사용자가 원하는 구간의 크기를 입력받아 히스토그램 구현하기
```
 
# 2. 데이터 구조 설계 (어떤 변수에 무엇이 들어가는지)
학생 점수 데이터는 리스트, 통계 지표(기초통계량)은 딕셔너리
- students = [ score1, score2 , score3, …]
- stats = {"min":  최소값, "q1":  제 1사분위수, "med": 중앙값, "q3": 제 3 사분위수, "max": 최대값, "iqr": IQR값(사분위수 범위)}

# 3. 함수별 의사코드 (3-5개 함수)

```
def student_data():
    # 1. '직접 입력' 으로 점수 입력받는다.
    # 2. 데이터 유효성 검사(숫자 여부, 0~100 범위 확인)를 수행한다.
    # 3. 정상적인 학생 데이터 리스트를 반환한다.
    pass

def statistics(students):
    # 1. 점수 리스트를 오름차순으로 정렬한다.
    # 2. 평균을 계산한다.
    # 3. 중앙값(제2사분위수) 및 제1, 제3사분위수를 계산한다.
    # 4. IQR(제3사분위수 - 제1사분위수)을 계산한다.
    # 5. 계산된 통계 지표들을 출력한다.
    # 6. 박스플롯 구현을 위해 계산된 통계 지표(딕셔너리)를 반환한다. 
    pass

def histogram(students):
    # 1. 20점 단위로 구간을 나눈다.
    # 2. 각 구간에 포함되는 학생 수를 센다.
    # 3. 별표(*)를 사용하여 히스토그램 형식으로 출력한다.
    pass

def boxplot(stats):
    # 1. 전달받은 통계지표(stats)를 활용하여 기초 통계량의 위치를 계산한다.
    # 2. 기호를 사용하여 CLI 환경에 박스플롯을 출력한다.
    pass
```

# 4. 화면 흐름 스케치 (손그림 사진)
